dependencies:
  pre:
    - chmod a+x ci-prepare.sh
    - chmod a+x ci-test.sh
    - bin/ci/prepare.sh
  cache_directories:
    - ~/dependencies
    - ~/.mix
    - _build
    - deps

test:
  override:
    - bin/ci/tests.sh

deployment:
  staging:
    branch: master
    heroku:
      appname: polar-temple-67773
